@model Kermesse.Models.RolUsuario

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Eliminar</h2>

<h3>¿Está seguro de borrar este registro?</h3>
<div>
    <h4>RolUsuario</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Rol1.rolDescripcion)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Rol1.rolDescripcion)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Usuario1.userName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Usuario1.userName)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

<div class="form-actions no-color">
    <button type="submit" value="Delete" class="btn btn-danger">
        <i class="fa fa-trash icon" style="margin-right: .5rem;"></i>
        Eliminar
    </button>
    <div class="btn btn-info">
        <i class="fa fa-arrow-left icon" style="margin-right: .5rem;"></i> @Html.ActionLink("Regresar", "Index", null, new { @style = "color: white;" })
    </div>
</div>
    }
</div>
