@model Kermesse.Models.Rol

@{
    ViewBag.Title = "Rol";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Rol</h1>

<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.rolDescripcion)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.rolDescripcion)
        </dd>


    </dl>

    <div class="form-group">
        <div class="col-12 col-md-6 col-lg-3 col-xl-2">
            @Html.ActionLink("Editar", "Edit", new { id = Model.idRol }, new { @class = "btn btn-success w-100" })
        </div>
        <div class="col-12 col-md-6 col-lg-3 col-xl-2">
            @Html.ActionLink("Regresar", "Index", null, new { @class = "btn btn-primary w-100" })
        </div>
    </div>
</div>